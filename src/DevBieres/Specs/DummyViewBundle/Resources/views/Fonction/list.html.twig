<table class="table table-hover" >
    {% for o in cols %}
       <tr>
             <td> 
                 <a href="{{path('fonction_edit', { docid:document.id, id:o.id})}}" > {{ o }} </a> 
             </td>
             <td>
                 {{ 'fonction.createdVersion'|trans }} : <strong> {{ o.versionCreated }} </strong>
             </td>
             <td class="one-action">
                <a href="{{path('fonction_delete', { docid:document.id, id:o.id })}}" class="btn btn-primary" > {{ 'trash-o'|ico }} </a>
             </td>
       </tr>
    {% endfor %}
</table>

